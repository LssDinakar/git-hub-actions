name: First name
on: workflow_dispatch
jobs:
  First-job: 
    runs-on: ubuntu-latest
    steps:
      - name: OS-Name
        run: echo "/etc/os-release"
      - name: HostName
        run: echo "$HOSTNAME"
